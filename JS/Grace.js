const fs = require('fs')

a = (function () {
	/*
		comment
	*/
	fs.writeFile("Grace_kid.js", "const fs = require('fs')\n\na = (" + a + ")\na()", err =>{
		if (err)
			console.error(err);
	})
	// return 
})
a()